openapi: 3.2.0
info:
  title: Netlify Purge API
  termsOfService: https://www.netlify.com/legal/terms-of-use/
  x-logo:
    url: netlify-logo.png
    href: https://www.netlify.com/docs/
    altText: Netlify
  version: '1.0'
  description: 'Operations tagged purge across 2 of this provider''s published API definitions: netlify-openapi-2.57.0-swagger.json, netlify-purge-api-openapi.yml. Each path carries the servers of the definition it was published in.'
servers:
- url: https://api.netlify.com/api/v1
security:
- netlifyAuth: []
tags:
- name: purge
  x-displayName: Cache purge
paths:
  /purge:
    post:
      operationId: purgeCache
      description: Purges cached content from Netlify's CDN. Supports purging by Cache-Tag.
      tags:
      - purge
      responses:
        '202':
          description: OK
        '400':
          description: Invalid request parameters
        '404':
          description: Site not found
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                site_id:
                  type: string
                site_slug:
                  type: string
                cache_tags:
                  type: array
                  items:
                    type: string
        required: true
    servers:
    - url: https://api.netlify.com/api/v1
components:
  schemas:
    purge:
      type: object
      properties:
        site_id:
          type: string
        site_slug:
          type: string
        cache_tags:
          type: array
          items:
            type: string
  securitySchemes:
    netlifyAuth:
      type: oauth2
      flows:
        implicit:
          scopes: {}
          authorizationUrl: https://app.netlify.com/authorize
externalDocs:
  url: https://www.netlify.com/docs/api/
  description: Online documentation
x-refined-from:
- netlify-openapi-2.57.0-swagger.json
- netlify-purge-api-openapi.yml
x-tagGroups:
- name: OAuth
  tags:
  - ticket
  - accessToken
- name: User accounts
  tags:
  - user
  - accountMembership
  - member
  - accountType
  - paymentMethod
  - auditLog
- name: Site
  tags:
  - site
  - environmentVariables
  - file
  - metadata
  - purge
  - snippet
- name: Domain names
  tags:
  - dnsZone
  - sniCertificate
- name: Deploys
  tags:
  - deploy
  - deployedBranch
  - deployKey
- name: Builds
  tags:
  - build
  - buildLogMsg
- name: Dev servers
  tags:
  - devServer
- name: Webhooks and notifications
  tags:
  - hook
  - hookType
  - buildHook
  - devServerHook
- name: Services
  tags:
  - service
  - serviceInstance
- name: Functions
  tags:
  - function
- name: Forms
  tags:
  - form
  - submission
- name: Split tests
  tags:
  - splitTest
- name: Large media
  tags:
  - asset
  - assetPublicSignature
x-original-swagger-version: '2.0'
