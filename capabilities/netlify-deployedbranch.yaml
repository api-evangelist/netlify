naftiko: 1.0.0-alpha2
info:
  label: Netlify Netlify's API documentation — deployedBranch
  description: 'Netlify Netlify''s API documentation — deployedBranch. 1 operations. Lead operation: deployedBranch. Self-contained
    Naftiko capability covering one Netlify business surface.'
  tags:
  - Netlify
  - deployedBranch
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    NETLIFY_API_KEY: NETLIFY_API_KEY
capability:
  consumes:
  - type: http
    namespace: netlify-deployedbranch
    baseUri: https://api.netlify.com/api/v1
    description: Netlify Netlify's API documentation — deployedBranch business capability. Self-contained, no shared references.
    resources:
    - name: sites-site_id-deployed-branches
      path: /sites/{site_id}/deployed-branches
      operations:
      - name: listsitedeployedbranches
        method: GET
        description: ''
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: site_id
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.NETLIFY_API_KEY}}'
  exposes:
  - type: rest
    namespace: netlify-deployedbranch-rest
    port: 8080
    description: REST adapter for Netlify Netlify's API documentation — deployedBranch. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/sites/{site-id}/deployed-branches
      name: sites-site-id-deployed-branches
      description: REST surface for sites-site_id-deployed-branches.
      operations:
      - method: GET
        name: listsitedeployedbranches
        description: listsitedeployedbranches
        call: netlify-deployedbranch.listsitedeployedbranches
        with:
          site_id: rest.site_id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: netlify-deployedbranch-mcp
    port: 9090
    transport: http
    description: MCP adapter for Netlify Netlify's API documentation — deployedBranch. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: listsitedeployedbranches
      description: listsitedeployedbranches
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: netlify-deployedbranch.listsitedeployedbranches
      with:
        site_id: tools.site_id
      outputParameters:
      - type: object
        mapping: $.
