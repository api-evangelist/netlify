naftiko: 1.0.0-alpha2
info:
  label: Netlify Netlify's API documentation — sniCertificate
  description: 'Netlify Netlify''s API documentation — sniCertificate. 2 operations. Lead operation: sniCertificate. Self-contained
    Naftiko capability covering one Netlify business surface.'
  tags:
  - Netlify
  - sniCertificate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    NETLIFY_API_KEY: NETLIFY_API_KEY
capability:
  consumes:
  - type: http
    namespace: netlify-snicertificate
    baseUri: https://api.netlify.com/api/v1
    description: Netlify Netlify's API documentation — sniCertificate business capability. Self-contained, no shared references.
    resources:
    - name: sites-site_id-ssl
      path: /sites/{site_id}/ssl
      operations:
      - name: showsitetlscertificate
        method: GET
        description: ''
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: site_id
          in: path
          type: string
          required: true
      - name: provisionsitetlscertificate
        method: POST
        description: ''
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: site_id
          in: path
          type: string
          required: true
        - name: certificate
          in: query
          type: string
        - name: key
          in: query
          type: string
        - name: ca_certificates
          in: query
          type: string
    authentication:
      type: bearer
      token: '{{env.NETLIFY_API_KEY}}'
  exposes:
  - type: rest
    namespace: netlify-snicertificate-rest
    port: 8080
    description: REST adapter for Netlify Netlify's API documentation — sniCertificate. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/sites/{site-id}/ssl
      name: sites-site-id-ssl
      description: REST surface for sites-site_id-ssl.
      operations:
      - method: GET
        name: showsitetlscertificate
        description: showsitetlscertificate
        call: netlify-snicertificate.showsitetlscertificate
        with:
          site_id: rest.site_id
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: provisionsitetlscertificate
        description: provisionsitetlscertificate
        call: netlify-snicertificate.provisionsitetlscertificate
        with:
          site_id: rest.site_id
          certificate: rest.certificate
          key: rest.key
          ca_certificates: rest.ca_certificates
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: netlify-snicertificate-mcp
    port: 9090
    transport: http
    description: MCP adapter for Netlify Netlify's API documentation — sniCertificate. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: showsitetlscertificate
      description: showsitetlscertificate
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: netlify-snicertificate.showsitetlscertificate
      with:
        site_id: tools.site_id
      outputParameters:
      - type: object
        mapping: $.
    - name: provisionsitetlscertificate
      description: provisionsitetlscertificate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: netlify-snicertificate.provisionsitetlscertificate
      with:
        site_id: tools.site_id
        certificate: tools.certificate
        key: tools.key
        ca_certificates: tools.ca_certificates
      outputParameters:
      - type: object
        mapping: $.
